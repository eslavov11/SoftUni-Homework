import java.util.Scanner;
public class CalcilateExpression{
	
	public static void main(String[] args){
		Scanner input = new Scanner(System.in);
		float a = Float.parseFloat(input.nextLine());
		float b = Float.parseFloat(input.nextLine());
		float c = Float.parseFloat(input.nextLine());
		System.out.print("F1 result: " + Math.pow(a*a + b*b)/(a*a - b*b),(a+b+c)/Math.sqrt(c)); 
		System.out.print("; F2 result: " +  Math.pow((a*a + b*b - c*c*c),(a-b)));
		System.out.println("; Diff:" + (((a+b+c)/3) - 
				((Math.pow(a*a + b*b)/(a*a - b*b),(a+b+c)/Math.sqrt(c) + Math.pow((a*a + b*b - c*c*c),(a-b))) /2));
		
//		((a2 + b2) / (a2 – b2))(a + b + c) / √c             (a2 + b2 - c3)(a – b)
	}
}